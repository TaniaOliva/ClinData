$kind: http-request
url: "{{baseUrl}}/api/pacientes/999999"
method: GET
scripts:
  - type: afterResponse
    code: |-
      pm.test('Status 404', function () {
          pm.response.to.have.status(404);
      });
    language: text/javascript
order: 7000
